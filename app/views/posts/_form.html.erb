<%= form_with(model: post, local: true, :url => forumthread_posts_path) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div id="post-body-entry" class="field">
    <%= form.label :body %>
    <%= form.text_area :body, size: "50x5" %>
  </div>

  <div id="post-submit-button" class="actions">
    <%= form.submit "Create Post" %>
  </div>
<% end %>
