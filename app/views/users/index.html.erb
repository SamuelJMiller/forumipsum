<h1>Users</h1>

<%= form_tag(users_path, method: :get) do %>
    <%= text_field_tag(:search, params[:search]) %>
    <%= submit_tag("Search") %>
<% end %>

<% @users.each do |user| %>
    <p class="user-link"><%= link_to user.username, user_path(user) %></p>
<% end %>
