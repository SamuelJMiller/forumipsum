<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :thread_id %>
    <%= form.number_field :thread_id %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :feedback_score %>
    <%= form.number_field :feedback_score %>
  </div>

  <div class="field">
    <%= form.label :is_banned %>
    <%= form.check_box :is_banned %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
